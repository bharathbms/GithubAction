name: jsonview

on: workflow_dispatch

jobs:
  job1:
    runs-on: ubuntu-22.04

    steps:
      - name: print github
        run: echo '${{ toJSON(github) }}'

      - name: print runner
        run: echo '${{ toJSON(runner) }}'
